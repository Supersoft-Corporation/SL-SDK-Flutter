name: softlink_flutter
description: "SoftLink Flutter SDK for deep linking, deferred deep linking, and install attribution."
version: 0.1.0
homepage: https://github.com/Usamakabir07/softlink_flutter
repository: https://github.com/Usamakabir07/softlink_flutter

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  device_info_plus: ^11.4.0
  get_storage: ^2.1.1
  app_links: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: